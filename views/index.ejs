<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Home page</title>
</head>
<body>
    <h2>You have entered values</h2>
    <p>Username: <%= query.username ?? "null" %></p>
    <p>Password: <%= query.password ?? "n/a" %></p>
    <p>Age: <% for (var i = 0; i < Number.parseInt(query.age); i ++) { %>
           <p> <%= `Age ${i}~` %></p>
        <%}%>
    </p>
</body>
</html>